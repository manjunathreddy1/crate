const files = await spCache.web.lists
  .getByTitle("Site Pages")
  .items
  .filter("ContentType eq 'News'")
  .select("Id", "Title", "FileRef", "ContentTypeId", "ContentType/Name")
  .expand("ContentType")
  .inFolder("/sites/Cadence/SitePages/Templates")();
