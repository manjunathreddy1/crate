import { Dialog, DialogType, IDialogStyles } from '@fluentui/react';

const dialogStyles: Partial<IDialogStyles> = {
  main: {
    width: '600px',
    height: '400px',
    maxHeight: 'none' // optional: prevents it from being restricted
  }
};

<Dialog
  hidden={false}
  onDismiss={() => {}}
  dialogContentProps={{
    type: DialogType.normal,
    title: 'Custom Size Dialog',
  }}
  styles={dialogStyles}
>
  <p>Your dialog content goes here.</p>
</Dialog>
